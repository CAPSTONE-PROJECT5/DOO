// ApprovalStatuses.js

const express = require('express');
const router = express.Router();

// Import ApprovalStatus model
const ApprovalStatus = require('../models/ApprovalStatus');

// Define routes for ApprovalStatuses resource...

module.exports = router;
