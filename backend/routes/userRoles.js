// UserRoles.js

const express = require('express');
const router = express.Router();

// Import UserRole model
const UserRole = require('../models/UserRole');

// Define routes for UserRoles resource...

module.exports = router;
