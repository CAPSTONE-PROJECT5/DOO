// DocumentTypes.js

const express = require('express');
const router = express.Router();

// Import DocumentType model
const DocumentType = require('../models/DocumentType');

// Define routes for DocumentTypes resource...

module.exports = router;
