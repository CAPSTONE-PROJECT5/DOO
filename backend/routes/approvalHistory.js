// ApprovalHistory.js

const express = require('express');
const router = express.Router();

// Import ApprovalHistory model
const ApprovalHistory = require('../models/ApprovalHistory');

// Define routes for ApprovalHistory resource...

module.exports = router;
